# CTS Mastery Quiz Native

This is a React Native / Expo scaffold for running the quiz natively on iOS.

Files:
- `App.tsx` - native quiz UI
- `quizData.json` - bundled question bank extracted from the HTML version

Run with Expo:
- `npm install`
- `npm run ios`

